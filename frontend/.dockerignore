# Dependencies
node_modules
npm-debug.log

# Build outputs
.next
out
dist
build

# Environment files
.env
.env.local
.env.*.local

# Git
.git
.gitignore

# IDE
.vscode
.idea

# Logs
*.log

# OS files
.DS_Store
Thumbs.db

# Testing
coverage

# Temporary files
*.tmp
.cache
```

---

## **Quick Explanation:**

### **.gitignore = "Don't push this to GitHub"**
```
node_modules/  ← 500MB folder, everyone installs it themselves
.env           ← Has passwords! NEVER push to GitHub
dist/          ← Generated files, rebuild them
```

### **.dockerignore = "Don't copy this into Docker"**
```
node_modules/  ← Docker will install fresh (COPY . . doesn't need it)
.env           ← Don't bake secrets into image
.git/          ← No need for git history in Docker